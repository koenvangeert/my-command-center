TASK 2: Add Database Indexes for Self-Review and Review-PR Queries
=====================================================================

COMPLETION EVIDENCE
===================

1. MIGRATION STEP ADDED
   File: src-tauri/src/db/mod.rs
   Lines: 495-516
   
   Four indexes created with IF NOT EXISTS pattern:
   - idx_self_review_comments_task_archived ON self_review_comments(task_id, archived_at)
   - idx_self_review_comments_task_round ON self_review_comments(task_id, round)
   - idx_review_prs_updated_at ON review_prs(updated_at DESC)
   - idx_review_prs_repo ON review_prs(repo_owner, repo_name)

2. TEST ADDED
   File: src-tauri/src/db/mod.rs
   Lines: 662-697
   Test: test_indexes_created_on_migration
   
   Verifies all 4 indexes exist in sqlite_master after migration runs.
   Test result: PASSED ✓

3. BUILD VERIFICATION
   Command: cargo build --manifest-path src-tauri/Cargo.toml
   Result: SUCCESS ✓
   Warnings: 42 (pre-existing, not related to changes)

4. TEST VERIFICATION
   Command: cargo test --bin ai-command-center db::tests
   Results:
   - test_database_initialization ... ok
   - test_indexes_created_on_migration ... ok
   - test_migration_does_not_overwrite_existing_global ... ok
   - test_migration_copies_credentials_to_global ... ok
   
   Total: 4 passed; 0 failed ✓

5. GIT COMMIT
   Commit: 45ecf62
   Message: perf(backend): add database indexes for self-review and review-pr queries
   Files changed: 1 (src-tauri/src/db/mod.rs)
   Insertions: 60

QUERY ANALYSIS
==============

Self-Review Queries (from src-tauri/src/db/self_review.rs):
- get_active_self_review_comments: WHERE task_id = ?1 AND archived_at IS NULL
  → Indexed by: idx_self_review_comments_task_archived(task_id, archived_at)
  
- get_archived_self_review_comments: WHERE task_id = ?1 AND archived_at IS NOT NULL
  → Indexed by: idx_self_review_comments_task_archived(task_id, archived_at)
  → Also uses: round = (SELECT MAX(round) FROM self_review_comments WHERE task_id = ?1 AND archived_at IS NOT NULL)
  → Indexed by: idx_self_review_comments_task_round(task_id, round)

Review-PR Queries (from src-tauri/src/db/review.rs):
- get_review_prs: ORDER BY updated_at DESC
  → Indexed by: idx_review_prs_updated_at(updated_at DESC)
  
- Potential repo filtering: repo_owner, repo_name
  → Indexed by: idx_review_prs_repo(repo_owner, repo_name)

MIGRATION SAFETY
================

✓ All indexes use CREATE INDEX IF NOT EXISTS
✓ No existing migration steps modified
✓ No table structure changes
✓ Backward compatible with existing databases
✓ Idempotent - safe to re-run on existing databases

PERFORMANCE IMPACT
==================

Expected improvements:
- Self-review comment queries: Eliminates full table scans on self_review_comments
- Review PR queries: Eliminates full table scans on review_prs
- Reduces review pane jank from database query latency
- Minimal storage overhead (indexes are small for these tables)

TASK COMPLETION
===============

All requirements met:
✓ Migration step added to src-tauri/src/db/mod.rs
✓ 4 indexes created with IF NOT EXISTS pattern
✓ Test verifies indexes exist in sqlite_master
✓ cargo build passes
✓ cargo test passes (all 4 db tests)
✓ Git commit created
✓ Evidence saved
