TASK 3: Deduplicate GeneralCommentsSidebar Comment Loading
==========================================================

OBJECTIVE
---------
Prevent GeneralCommentsSidebar from making redundant IPC calls when parent 
SelfReviewView already loaded the same data into stores.

CHANGES MADE
------------

1. src/components/GeneralCommentsSidebar.svelte
   - Modified loadComments() function (lines 45-63):
     * Added force parameter: async function loadComments(force = false)
     * Added guard at start: if (!force && stores have data) return
     * Prevents IPC calls when stores already populated
   
   - Modified handleAdd() function (lines 65-83):
     * Changed from direct IPC call to loadComments(true)
     * Ensures forced reload after adding comment
     * Maintains refresh behavior for add/delete operations

2. src/components/GeneralCommentsSidebar.test.ts (NEW)
   - 5 test cases covering:
     * Guard prevents IPC when stores populated
     * IPC called when stores empty
     * Forced reload works after add operation
     * Empty state renders correctly
     * Comments render when stores have data
   - Uses vi.mock() for IPC function mocking
   - Follows project test patterns (Toast.test.ts)

VERIFICATION
------------

✓ pnpm build
  - Frontend compiles cleanly
  - No TypeScript errors
  - Output: 1.6MB JS, 142KB CSS

✓ pnpm test
  - All existing tests pass (no regressions)
  - New test file included in test suite
  - 5 new test cases all passing

✓ Git commit
  - Message: "perf(frontend): skip redundant comment loading in GeneralCommentsSidebar"
  - Includes detailed description of changes
  - Commit hash: eb800da

PERFORMANCE IMPACT
------------------

Before: 2 IPC calls on mount (getActiveSelfReviewComments + getArchivedSelfReviewComments)
After:  0 IPC calls on mount (guard prevents redundant calls)
        2 IPC calls on add/delete (forced reload via loadComments(true))

Result: Eliminates double network roundtrips on component mount when parent 
        already loaded data into stores.

GUARD PATTERN DETAILS
---------------------

Guard condition: if (!force && ($selfReviewGeneralComments.length > 0 || $selfReviewArchivedComments.length > 0))
- Checks BOTH stores (general and archived comments)
- Only skips if EITHER store has data
- Forced reload bypasses guard for refresh operations
- Maintains backward compatibility with existing code

TESTING COVERAGE
----------------

Test 1: skips IPC calls when stores already have data
  - Pre-populate stores with mock data
  - Render component
  - Verify IPC functions not called

Test 2: calls IPC when stores are empty
  - Clear stores
  - Render component
  - Verify IPC functions called with correct taskId
  - Verify stores populated with returned data

Test 3: forces reload when add comment is clicked
  - Pre-populate stores
  - Clear mocks
  - Render component (guard prevents IPC)
  - Click add button
  - Verify IPC called (forced reload)

Test 4: renders empty state when no comments
  - Mock IPC to return empty arrays
  - Verify empty state message displayed

Test 5: renders comments when stores have data
  - Pre-populate stores
  - Verify comments rendered
  - Verify archived section visible

NOTES
-----

- Guard pattern is simple and effective
- Force parameter allows explicit refresh when needed
- No changes to store types or IPC function signatures
- Backward compatible with existing code
- Follows project conventions for testing and component structure
